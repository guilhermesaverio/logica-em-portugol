Algoritmo "Soma"

Var
     contador, soma: inteiro
Inicio
      contador <- 500
      soma <- 0
      enquanto contador >= 0 faca
      escreval(contador)
      soma <- soma + contador
      contador <- contador - 50
      fimenquanto
      
      escreva("soma: ",soma)

Fimalgoritmo
